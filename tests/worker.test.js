/*
.Example
npm test -- worker.test.js

run the tests in this file.

.Example
nvm exec v16 npm test -- worker.test.js

run the tests in this file using nvm (node version manager)
*/

describe('Worker', () => {

    describe('constructor', () => {
  
      it('has a parameter named parent that is an instance of AdpClient', () => {  
      });
  
    });
  
    describe('all()', () => {
        it('returns an array of worker objects', () => {
        });
    });

    describe('one()', () => {
        it('has a parameter named associateOID', () => {
        });

        it('returns a worker object', () => {
        });
    });

    describe('hire()', () => {

    });

    describe('rehire()', () => {
    });

    describe('terminate()', () => {
    });

  });